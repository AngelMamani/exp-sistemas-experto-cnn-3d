[project]
name = "cnn3d-expert-exposicion"
version = "1.0.0"
description = "Presentación web CNN-3D + Sistemas Expertos — UNAMAD"
requires-python = ">=3.12"

[tool.vercel]
entrypoint = "cnn3d_expert.wsgi:application"

[tool.vercel.scripts]
build = "python manage.py collectstatic --noinput"
